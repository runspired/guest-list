import promiseToken from 'guest-list/utils/promise-token';
import { module, test } from 'qunit';

module('Unit | Utility | promise-token', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = promiseToken();
    assert.ok(result);
  });
});
